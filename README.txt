Pour lancer un serveur PHP local (pour peut que PHP soit bien installé):

Dans l'invite de commande:
Se mettre à la racine du dossier et taper:
php -S localhost:8000

Consulter ensuite http://localhost:8000/